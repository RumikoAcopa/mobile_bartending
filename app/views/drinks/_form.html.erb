<% if drink.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(drink.errors.count, "error") %> prohibited this 
    <%= object.class %> from being saved:
    </h2>
 
    <ul>
    <% drink.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>